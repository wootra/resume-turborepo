import { twMerge as tw } from 'tailwind-merge';

export const cardClass = tw(
    'flex items-center justify-center w-32 h-16 rounded-xl shadow-lg bg-slate-200 hover:bg-lime-300 transition-all duration-500 hover:mt-2'
);
