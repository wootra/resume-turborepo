--- 
import {twMerge} from 'tailwind-merge';
interface Props {
	className?: string;
	id?: string;
}

const { className, id } = Astro.props;
---
<div class={twMerge("flex flex-col justify-center items-center", className)} id={id}>
	<slot />
</div>
	