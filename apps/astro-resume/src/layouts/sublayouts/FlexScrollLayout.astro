--- 
import {twMerge} from 'tailwind-merge';
interface Props {
	className?: string;
	id?: string;
}

const { className, id } = Astro.props;
---
<div class={twMerge("flex flex-col justify-top overflow-y-auto w-full", className)} id={id}>
	<slot />
</div>
	